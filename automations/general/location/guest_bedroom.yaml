- id: 'guest_air_freshener'
  alias: Turn On Air Freshener When There Are Scheduled Visitors
  description: ''
  trigger:
  - entity_id: input_boolean.visitors
    platform: state
  condition: []
  action:
  - service_template: "{% if is_state('input_boolean.visitors', 'on') %} switch.turn_on {% else %} switch.turn_off {% endif %}"
    data:
      entity_id: switch.guest_air_freshener
